---
title: "Webhook limit"
description: "How to handle the configurable webhook limit"
---

There is a limit on configurable webhooks per integration.

When you need operation-specific URLs, prefer sending `callbackUrl` directly in the transaction, subscription, or transfer creation request.
